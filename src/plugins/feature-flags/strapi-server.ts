/**
 * Feature Flags plugin — server entry point.
 * No server-side logic is needed; the token API lives in
 * `src/api/feature-flag-token`. This file only registers the plugin so the
 * admin part is loaded.
 */
export default () => ({});
